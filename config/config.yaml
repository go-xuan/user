# 当前应用配置
server:
  name: user
  port: 9001
#  env: localhost
  prefix: /user/api/v1

# nacos访问配置
nacos:
  address: localhost:8848
  path: /nacos
  username: nacos
  password: nacos
  mode: 0
  nameSpace: localhost
  logDir: resource/.nacos/log
  cacheDir: resource/.nacos/cache
  loadConfig:
    init: server.yaml,database.yaml,redis.yaml
