# 当前应用配置
server:
  name: user
  port: 9001
  prefix: /user/api/v1
  debug: true

# nacos访问配置
nacos:
  address: localhost:8848
  path: /nacos
  username: nacos
  password: nacos
  mode: 0
  nameSpace: localhost
